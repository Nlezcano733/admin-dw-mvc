<main class="container grid grid-cols-12 auto-rows-min gap-y-4 mx-auto py-4 px-3 gap-x-4 md:gap-x-8 md:border">
  <div class="col-span-12 h-12 flex flex-wrap justify-between items-center border-b">
    <h3 class="text-2xl font-bold text-gray-900">Product Detail</h3>
  </div>

  <section class="col-span-12 md:col-span-4">
    {{#with data}}
    <form id="addProduct" action="edit/{{parseString _id}}" method="post">
      {{> input type="text" label="Title" defaultValue=title}}

      <div class="my-2">
        <label class="block mb-2 text-md font-medium text-gray-900">
          Brand
        </label>
        <input type="text" name="brand" list="brands" value="{{brand}}"
          class="block px-2 py-1 w-full text-gray-900 bg-gray-50 rounded-lg border border-gray-300 sm:text-sm focus:ring-blue-500 focus:border-blue-500" />

        <datalist id="brands">
          {{#each brands}}
          <option value={{brands}}>{{brands}}</option>
          {{/each}}
        </datalist>
      </div>

      {{> textarea label="Description" defaultValue=description}}
      {{> inputBtn type="number" label="Price" symbol="$" defaultValue=price}}
      {{> input type="number" label="Stock" defaultValue=stock }}
      {{> select label="Type" defaultValue=type defaultValue=type }}

      <div class="my-2">
        <label class="block mb-2 text-md font-medium text-gray-900">
          Category
        </label>
        <input type="text" name="category" list="categories" value="{{category}}"
          class="block px-2 py-1 w-full text-gray-900 bg-gray-50 rounded-lg border border-gray-300 sm:text-sm focus:ring-blue-500 focus:border-blue-500" />

        <datalist id="categories">
          {{#each categories}}
          <option value={{categories}}>{{categories}}</option>
          {{/each}}
        </datalist>
      </div>

      {{> input type="text" label="Picture" defaultValue=picture}}
      {{! para picture se usara la opcion de input type file}}

      <div class="flex flex-wrap justify-end mt-4">
        {{> btnOutline label="Reset" type="reset" color="blue" }}
        {{> btn label="Edit product" type="submit" color="blue" }}
      </div>
    </form>
    {{/with}}

  </section>

  <section class="col-span-12 md:col-span-8 h-full">
    {{#with data}}
    <section class="text-gray-600 body-font overflow-hidden">
      <div class="flex justify-center items-center h-36 md:h-48 xl:h-72 border">
        <img alt="ecommerce" class="w-auto h-full object-cover object-center" src={{picture}}>
      </div>

      <div class="mt-4 lg:mt-8">
        <div class="flex justify-between">
          <h2 class="text-sm title-font text-gray-500 uppercase tracking-widest">{{brand}}</h2>
          <p class="text-sm text-gray-500 uppercase">{{type}} | <span class="normal-case">{{category}}</span></p>
        </div>
        <h1 class="text-gray-900 text-xl lg:text-2xl title-font font-medium my-2 lg:mb-4">{{title}}</h1>


        <p class="w-full max-h-64 overflow-y-auto mb-4">{{description}}</p>


        <div class="w-1/2 flex justify-between">
          <span class="title-font font-medium text-xl text-gray-900">${{price}}</span>
          <span class="font-medium text-lg text-gray-500">Stock: {{stock}}</span>
        </div>

      </div>

    </section>
    {{/with}}
  </section>

</main>